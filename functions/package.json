{
  "name": "roipayroll-functions",
  "private": true,
  "main": "lib/index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "tsc",
    "serve": "npm run build && firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^5.1.1",
    "node-fetch": "^2.6.9"
  },
  "devDependencies": {
    "@types/node": "^20.17.0",
    "@types/node-fetch": "^2.6.12",
    "typescript": "^5.6.3"
  }
}
